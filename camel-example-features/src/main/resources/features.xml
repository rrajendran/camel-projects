<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0" name="${project.artifactId}-common-${project.version}">
    <repository>mvn:org.apache.camel.karaf/apache-camel/${camel.version}/xml/features</repository>
    <feature name="camel-example-blueprint" version="${project.version}">
        <bundle>mvn:${project.groupId}/camel-example-blueprint/${project.version}</bundle>
    </feature>

</features>